---
# vars file for andock-ci-build
build_path: .

checkout_path: "{{ build_path }}"

push_path: "{{ build_path }}/andock-push"

target_branch_suffix: "-build"

target_branch: "{{ branch }}{{ target_branch_suffix }}"
